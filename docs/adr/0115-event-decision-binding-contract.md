# 0115: 决策↔事件绑定契约——推送时钉 event_id、fail-loud、一事件一裁断

Status: Accepted（PR #719 merged `4ffecc42` 2026-07-08，线上 3 轮收敛；决策：2026-07-08 用户授权清账收口时拍定方向〔#387 处置评论方案 A 为主＋B 的推送时钉键〕，搭 #87 宗藩闸 PR 走评审；承 #387/#345/#391、ADR 0014 离散键 fail-loud 纪律、0043/0049/0052 呈现壳）

玩家月末 HITL 事件选择的**绑定真源＝推送时钉定的 event_id**（权威候选快照/推送期分配的稳定键），**不依赖 simulator 回显**（回显缺失即 #345 旧 bug 之源——绑定由构造保证，非由文本捞回）。三条配套：

1. **非事件类选择同样 durable 落库**（P1）——不因无 event_id 而丢弃（playtest F12-B「事件选择不落库」的根治面）。
2. **无法绑定 fail-loud**：缺键且快照不唯一 → 响亮失败（对齐 0014 离散键纪律：白名单校验、归一失败响亮、绝不兜底写未支撑值），不静默猜。
3. **一事件一裁断**：`event_triggers` 语义＝first-wins 去重（#391 last-write-wins bug 同契约收口）；数组化 schema **仅当**玩法确需一事件多次裁断才升级，当前不建。

呈现对齐 **0093 案头**（与 M12 #696 打回条目同一案头，不另起 UI）。实现票＝#387 转切片，#391 作同契约条款一并收口。

**毙案**：靠 simulator 回显 event_id 绑定＝把离散键交给叙事文本往返，违 0014 纪律，毙；无法绑定时兜底写「最近似事件」＝写未支撑值，毙。
