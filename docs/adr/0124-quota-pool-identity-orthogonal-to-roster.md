# 0124. 池 = 配额/账单边界，与模型花名册正交

Date: 2026-07-10

## Status

Proposed

## Decision

编排器的「池」只定义为**配额/账单观测维度**，模型定义为**能力维度**。选座只读取 ADR 0134 按“接入渠道 + 模型”给出的固定席位顺序；池不改写固定席位顺序。

## Consequences

配额观测与模型能力保持正交，固定席位选择不由池状态改写。
