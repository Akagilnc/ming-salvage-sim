# W3 开工交接 · 2026-08-09

来源：当日三路只读扫描（`archive/scans-2026-08-09/w3-scan-{A,B,C}-*.md`），数据以 GitHub issue/PR 实查为准。
用途：后续 session 续跑 W3 时从本文档开工，不必重新盘点。排程真源仍是 **#486 body**（本文档是快照，若冲突以 #486 body 为准）。

---

## 1. 总盘

W3-4 联合实施波三家族，共 44 片，**完成 6 片**：

| 家族 | PRD | 完成 | 已合片 |
|---|---|---|---|
| #471 皇帝动作语法 | #513 | **3/16** | #515(S0)、#527(S11)、#1123(S15)，经 PR #1120 + hotfix #1134 |
| #478 呈现·节奏·回声 | #534 | **3/13** | #536(S6)、#537(S9)、#538(S11)，经 PR #791/#785/#783 |
| #474 旨意承诺与执行建制 | #556 | **0/15** | 无；#571 有未合 PR #1146 |

调度规则（#486 body 原文精神）：只认切片原生 `blocked_by` + `ready-for-agent` 标签，不按家族名硬串；允许大并发、保持小 PR；家族批次用 RFA 控制 admission；单一评审主题收敛即 ship/merge。

## 2. 关键路径（当前卡点和解锁链）

```
#571 (474·S1 案卷底座)          OPEN · RFA · 无依赖 · 有 OPEN PR #1146（Closes #571）
  └→ #560 (474·S5 颁布关管线)   OPEN · RFA · 被 #571 挡
       └→ 合入后解锁 #471 侧：#517 #518 #520 #521 #524 #528（均 OPEN、无 RFA）
            └→ #518 → #519 → #529（无 RFA）与 #568（RFA，另被 #571 挡）
```

**当前瓶颈就是 #571 的 PR #1146 没合。** 续跑第一件事：看 #1146 状态（在跑/评审/停滞），推它合入。

## 3. 现在就能开工的票（扫描日口径）

判据：OPEN + `ready-for-agent` + 无 OPEN 前置。

1. **#571** — 474·S1 案卷底座（已在 PR #1146 上，接续而非重开）
2. **#539** — 478·S1 场卷轴读契约 + 最小卷轴视图（RFA、前置全清；它是 #478 家族后续 #540-#548 的公共前置，价值仅次于 #571）

## 4. 三个坑（交接重点）

1. **#522 漏交付**：#486 body 把 #522(S6 招抚) 写进 #1120 批次，但 PR #1120 实际未交付它。#522 仍 OPEN、带 RFA——但它原生 blocked_by 挂 #560，所以即便有 RFA 也得等 #560。开工前先把 body 批次记述与实际交付对齐（改 body 或在 #522 注明）。
2. **#516/#523/#525/#526 前置已清但无 RFA**：四片（问/令查分界回归、收权罢差、留中、收夜口令）依赖已全部 closed，但都没挂 `ready-for-agent`。按调度规则它们不能进批次——要么补标签（确认票面已 specified），要么明确它们是 human 活。
3. **没有 W3 续跑坐标**：`NEXT_ORDER.md` 唯一的续跑资产段是 W2 的 #497，W3 各家族**没有** ledger / family 分支起点 / 编排器续跑脚本记录。#471 的 `family/513` 分支已交付完（本地+远程都在，可删）；#474 的 `origin/family/571` 存在（HEAD ae41aa0f，2026-07-26），无本地 worktree。续跑 #474 时先确认 #1146 用的什么编排资产，把坐标补进 NEXT_ORDER.md 或 #486 评论。

## 5. 工作树卫生（与 W3 开工相关）

- W3 相关 worktree：**0 个**。61 个 worktree 里没有任何挂在 #515-#529/#560/#571 或三家族分支上的。
- **21 个 feat/fix/takeover worktree 对应 issue 全部 CLOSED 且已合 PR**，可清（清单见 `w3-scan-C-worktrees.md` §2）。
- **11 个 prunable**（gitdir 指向已不存在），`git worktree prune` 可收。
- exam/* 实验树 29 个，按需自行处置。

## 6. 各家族余片速查

**#471（余 13）**：#517 #518 #519 #520 #521 #522(RFA) #523 #524 #525 #526 #528 #529 #516 —— 全部依赖链收敛到 #560（#516/#523/#525/#526 除外，见坑 2）。
**#478（余 10）**：#539(RFA,根) → #540/#541/#542/#544/#545/#546 → #543 → #547 → #548；另 #536/#537/#538 已合。
**#474（余 15 全余）**：#571(根) → #557/#558/#559/#560 → #561/#563/#566 → #562/#564/#565/#567/#568 → #569 → #570(族尾验收)。

## 7. 续跑第一天建议动作

1. 查 PR #1146 状态，推合 #571。
2. #571 合后：#560 进批次（RFA 已备）；同时 #539 可并行开。
3. 处理坑 1（#522 批次记述对齐）与坑 2（四片补 RFA 或转 human）。
4. #560 合后：#471 侧六片解锁，按 RFA 补齐后大并发开跑，小 PR 逐片 ship。
5. 顺手清 21 个死 worktree + `git worktree prune`。
