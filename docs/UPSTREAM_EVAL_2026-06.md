# 上游批量评估结论（2026-06 批次）

> **状态：正式（2026-06-12 入库）。** 落点 `docs/UPSTREAM_EVAL_2026-06.md`；#75 及 #21–43 子 issue 按第六节处置清单执行。
> 初稿 2026-06-10（评估基线 = 上游 GitHub 当日 HEAD 21c0bca）；2026-06-12 修订：对齐 PR #90/#94/#95（ADR 0008 PR1/契约 0009/ADR 0008 PR2）落地后的现状，补 #28/#35/#36/#37 处置。
> 本文档面向公开仓库，措辞规则：只写做法与实现，上游仅中性 commit 引用。

## 评估基准（用户 2026-06-10 拍板）

本仓与上游已无同步关系。**默认无视；特别好的才采纳。** 采纳的项按本仓标准（fail-loud + TDD）实现。

**评估时效窗注记（2026-06-12）**：上游发布以构建分发为主，GitHub 仓库更新滞后（自 2026-06-10 后无新 commit，同期已有多个新版本对外发布）。按 GitHub commit 所做的评估只反映其镜像时点，不反映上游实际进度——这是「按需查阅、不做周期性摸底」基准的又一依据。

## 一、采纳（3 项）

| 项 | 做法 | 实现 |
|---|---|---|
| #23 | 5 条落库不变式：① 钱按账本单位归一（200两→0.02万两，防千倍错）② 财政改动无 mode 拒收、叙事矛盾拒收 ③ 新立科目当月进预算账本 ④ 禄米削减按月额算+政治反噬必落盘 ⑤ 调任带 location 时 DB 列+内存对象+office_changes 三处一致 | **先审计后补缺**（2026-06-12 修订）：PR #95（ADR 0008 PR2）已把 fiscal/army/power 等段迁入逐项拒收契约，第 ② 条很可能已被覆盖或半覆盖——落实第一步=逐条审计 5 条不变式 vs PR2 后的现状，只对缺口补 pytest，不重复造轮子。**第 ⑤ 条整条并入 #13**（ADR 0009 行止模型 `location+transit_to` + 决定 6「三面同步」契约正是此事，且该 ADR 簿记明文「#23 不变式⑤进测试集」；C1 实现已开工，单独立项必撞车）。第 ④ 条落实时注意（用户 2026-06-10 拍）：结构性科目（宗禄等）一道圣旨直接砍**允许**，但必须伴随严重政治代价且**代价必落盘**；⚠️ 宗藩/藩王代价的具体形态悬置待 #87 线设计，此处只做「代价必落盘」最小口径 |
| #27 | 地区 `controlled_by` 落库守门：值必须在 `powers.id` 白名单内 | 落库前查白名单，非法值显式拒收（ADR 0008 口径，产 RejectedItem 走 PR2 收集器），配测试 |
| cb20081 单摘 | `build_simulator_context` 输出字段按稳定前缀重排，提示词缓存友好 | 几行排序调整，不改语义；spark/codex 后端直接受益。**落点更新（2026-06-12）**：#13（ADR 0009 实现）本就要动盘面 TSV（人才池视图接入），随该 PR 顺手做，零额外成本 |

## 二、记档、暂不动手（遇到对应问题时再处理）

- **#21 HITL 状态门**（人物抉择先查 status/location 的 prompt 规则）——**依赖写入侧先修好**：当前病根是 status/location 不更新（#11/#12 仍 OPEN、#86 location 全空），数据错误时「先查再裁」查到的也是错数据，反而给幻觉盖「已核验」的章。排 #86/M1 之后；**#13（ADR 0009 实现）落地后写入侧即修复，届时重估**。
- **#22 军队归零撤番号**——**并入 #44 一起设计**（同一个兵力生命周期的上下界：#44 管狂扩军上界、#22 管兵打光下界）。#44 把应发饷改为按兵力派生后，「零兵吃饷」自动消失；剩余的「0 兵幽灵番号仍进裁判盘面」一条并入 #44 验收清单即可，#22 不单独立项。
- **#31 minister_recap**——月末把被召见大臣的奏对压成 120 字纪要、下月召见**该大臣**时注入其 system（每人一份，非全员共享）。对话原文存储（chat_messages）不变；补的是「大臣记不得自己上月说过什么」——现有跨月注入只有章节记忆（全局朝局），无个人记忆层。在本仓 memories 层实现、LLM 走探针后端。注：同 issue 的「对话历史 agno session 自管」部分 fork 已继承（session.py:561），无需决策。
- **#26 量化阈值**——3 个既有 auto-trigger 事件的 resolve/fail_condition 改量化阈值。**与 #12（事件缺结构化前提门）同设计**：#12 管触发侧、#26 管结案侧，一体两面（README「历史有门」），做 #12 时一并处理。
- **#32 部分**——tags 题材枚举、抄家分库听旨意（prompt 规则，与 P1「抄没→economy_moves」同向）可单取；部门/科技落成实体挂 #85 机制内审拍完再定（若采，剥离 #41 的 requires/default_unlocked）。**用户已拍扩展方向：人物加家产字段，抄家金额以家产为基准/上限**（README「账本有牙」+ P2 数据轴的直接应用，杜绝魔幻抄家几亿两）；抄家分库规则作其配套。落实时确认 characters 表现状并定家产是否对玩家可见（锦衣卫可查 vs 抄了才知道）。
- **#24/#25 探针工具**（delta 字段覆盖矩阵、court tool 触发率探针）——思路可借，要用时写薄版（#24 需补火器/城防炮 case、all_fields 对齐探针 schema）。

### 补遗处置（2026-06-12，初稿未显式覆盖；#36 的决策记录在第四节）

- **#28 direct_decree_probe --keep-hitl 开关**——探针调试便利，按「特别好才采」不够格；思路同 #24/#25「要用时写薄版」。关闭。
- **#35 office 授权改读 DB court_grant_json + 户部 adjust_tax 立 issue**——授权机制属 web court 通道，探针 CLI 现不走该路径；遇授权问题时再回看（若将来采纳，与 #36 的关键词授权去留联动评估）。记档关闭。
- **#37 fiscal_config 占位符补齐 + 局势数量上限闸**——前半属 M3 财政线参照素材（同 #33 注记）；后半「局势数量上限」是机械 clamp，与 P2「代码只 clamp」相容，遇局势爆表时再取。记档关闭。

## 三、搁置，挂 M3 财政线

- **#33 田亩财政重构**——v23.1 省级财政基座已覆盖官民田×亩率，并扩展三饷/火耗/起运存留/宗禄/逋赋/隐田维度；regions.json 的史实校准数据可作 #70 重标参照（以上游最新 7a2db8e 一轮为准）。
- **#34 grain_stock 迁移 + 年度粮食结算**——机制方向合理；等 #66 port 时与基座一起拍，现在动 regions schema 与 M1 撞车。
- **#43 金手指 vs 财政危机链（设计提醒）**——三级财政危机事件（太仓透支/国债滚积/信用崩裂，gate 国库≤-100）与悲剧定位同向，宜随 M3 基座一起评估；届时重审金手指：皇家天佑金矿 +800/月会使国库永远到不了触发线，整条「财政末路」叙事被屏蔽。建议：探针验证期保留，M3 落地时砍掉或缩水到象征量。

## 四、不采/关闭（决策记录）

- **#30 goal 字段：不取（设计否决，用户 2026-06-10 拍）。** 皇帝影响世界的方式只有下旨与密令（README「圣旨不是按钮」：一句话要经召见、拟旨、结算，可能执行、可能走样、可能被抵消）。goal 字段是场外方向盘——皇帝定个抽象目标、裁判每月朝它推，绕过大臣走样与政治代价，与核心循环相抵。皇帝想定方向，下旨即是。同 commit 其余三件（删 close_issues / inertia=0 / `_synth_resolve_effect` 兜底）涉结算核心，M1 重构时按本仓三结案路径现状自行裁决，不作为采纳项。
- **#29 db 拆包 + HITL 上下文内存化：不采。**
  - 拆包：「降低未来同步成本」在无同步关系前提下不成立；M1 若需要拆 db.py，按本仓契约设计模块边界。
  - HITL/resolve context 内存化：**有实证反例**——2026-06-10 服务死于 turn 24 结算中段（simulator 已出邸报、HITL 未答），重启后从 DB 无损接续在亲裁点，玩家零损失零重跑。resolve context 继续 DB 持有（ADR 0008 已加固毒 payload 防护）。
- **#38 Electron 打包**：关闭。分发阶段的事，探针走源码运行；届时再议。
- **#39 court-chat 朝会**：关闭（cb20081 字段重排已单摘，见采纳表）。朝会概念若要 CLI 版另立 issue。
- **#40**：关闭。加了又 revert，净零。
- **#36 技能卡 UI + 关键词授权下线**：不采，关闭（2026-06-12 补遗，纠正初判）。上游此变更是**删除**技能卡系统与关键词授权（`skills.json`/`skills.py`/`terminal.py` 技能卡查看均下线，授权改走 DB court_grant_json）；而本仓探针 CLI（`ming_sim/cli/terminal.py`）**现仍依赖**技能卡显示与关键词授权流——采纳=砍掉在用功能。若将来 #35 的 court_grant_json 授权替代在本仓落地，技能卡去留可随之再议。
- **#41 科技树前置 DAG**：关闭。与 P3 设计铁律冲突（拒绝科技树/长线文明建造，护「越努力越无解」的悲剧基调）；此结论宜在 docs 设计铁律处留一句备注。
- **#42 issue payload 前端来源字段**：关闭。2 行前端便利，按「特别好才采」基准不够格。

## 五、新批速扫（463881d..HEAD，24 commits / +16,080 行，摸底基线之后）

不开第二轮摸底，按需查阅。标记项：

- **c4929e4** 人物 location 全量初始化为 region_id ← **已被 ADR 0009 消化**（其行止模型条目明文引用此 commit 作初始化数据参照），#13 实现时直接用，无需另记。
- **军备三级重构**（2bc6b42/faa448a/d28e1c5/bb2bbfb：军→兵种→装备实物件数）← 与本仓 P2「数据轴 0-100 + LLM 软判」设计方向不同，无视。
- **7a2db8e** 兵种单价军费+辽饷亩率 ← 同时涉 P2 与 M3，#70 校准时参照其数据。
- **e8c067b** extractor 字段值校验 ← 与 #14 同向，做 #14 时可查阅其校验清单。
- **a8acb01** 格式契约中文化（2 文件小改）← 与本仓中文化同向，顺手时看一眼。
- 其余（剧本编辑器、触发 DSL、超时配置）：无标记。

## 六、处置执行清单（2026-06-12 定稿）

| 处置 | issue | 动作 |
|---|---|---|
| **保留改造** | #23 | 重定 scope：「审计 5 条不变式 vs PR #95 现状 → 缺口补 pytest」；第⑤条移交 #13（评论注明） |
| **保留** | #27 | 保持 open，小实现项（白名单守门+测试），排 M1 |
| **关闭·记档** | #21 #22 #24 #25 #26 #28 #31 #32 #35 #37 | 各留一条评论指向本文档对应行（含合并去向：#22→#44、#26→#12、#32→#85），关闭 |
| **关闭·挂 M3 注记** | #33 #34 #43 | 评论指向本文档第三节 + M3 epic（#65），关闭 |
| **关闭·不采** | #29 #30 #36 #38 #39 #40 #41 #42 | 评论指向本文档第四节决策记录，关闭 |
| **收口** | #75 | 评论总结（23 个子 issue 全处置：保留 2（#23 改造 + #27）/ 记档关闭 10 / M3 注记关闭 3 / 不采关闭 8；另有 cb20081 commit 单摘随 #13），指向本文档，关闭 |

另：cb20081 单摘随 #13 顺手做（已写进采纳表）；金手指去留时间表挂 M3（见 #43 行）。
