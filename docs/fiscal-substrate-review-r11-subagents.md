# r11 · 干净上下文 subagent(v11 + spike 代码)

> opus 4.8 / sonnet 4.6 冷读 v11 + **亲跑 spike**。2026-06-09。opus 做了变异测试。

## opus 4.8 — 不锁;但守恒框架对、检查器紧
- **真 bug(执行实证)**:§0.1 断言漏 `拨付net`。spike 的 省内可支 含拨付net,断言里没有 → opus 设 拨付net=20 跑出**残差+20 FAIL**。G1/G2 都 0 掩盖了。修:`Δcash=(民间流出+拨付net)−受款方流入`。
- **检查器够紧**:注入 8 个变异 bug(漏火耗实收/漏偿旧欠/漏起运/假注资/漂没翻倍…)全 FAIL 抓到,阈值 1e-6 合理。**不是假 PASS。**
- 旧 tautology 废得对(变异验证)。
- **半数对账账户在 code 里是死项**:中饱/eff损耗/追赃/挪借/清欠/蠲免/清丈 全零写入路径;漂没有码但 golden 率=0 没激活。守恒只在最窄 0 值路径上 PASS。
- claim 超额 clamp(max0)无声截断,现金守恒抓不到,无 golden。
- 须先修断言拨付net + 补 G3–G5 再锁。

## sonnet 4.6 — 基本锁,几处缝
- §0.1 主断言方向对、废 tautology 对;但「受款方流入」定义没把 ⓪ action 阶段补饷支出明确纳入。
- **C 对账是 ΣCASH 的子集分解,不是独立第三约束**(C 账户本身是 CASH),v11 没说清。
- spike 只断言现金主守恒,**缺债务对账断言**(三条对账只验了一条)。
- 实征不经显式 transfer 入省库(隐式假设),port 有风险。
- G3–G5(清丈/挪借/漂没-中饱-efficiency)缺,efficiency 语义未锁,这些路径有潜在破守恒洞。

## 横评:撞车
拨付net 漏项(opus 跑实)· 死账户/G3-G5 缺(opus+sonnet)· 缺债务对账断言(sonnet)· action 补饷在受款方流入定义未纳入(opus+sonnet)· C 是子集非独立约束(sonnet)。
**关键:这轮是「spike+执行+变异测试」逮到纸审逮不到的真 bug(拨付net),且证明检查器够紧 —— 验证循环成立了。**
